import styled from 'styled-components';

const PDPContainer = styled.div`
  display: flex;
  min-height: 550px;
`;

const PDPMain = styled.div`
  width: 100%;
`;

export const Styled = {
  PDPContainer,
  PDPMain,
};
